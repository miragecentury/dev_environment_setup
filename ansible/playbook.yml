---

- name: Auto-Setup Dev Environment
  hosts:
    - localhost
  connection: local
  gather_facts: true
  vars:
    username: "{{ lookup('env', 'USER') }}"
  roles:
    - role: homebrew
    - role: gh
    - role: fonts
    - role: zsh
    - role: security
    - role: kubernetes
